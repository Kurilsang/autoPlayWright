# 08: 快照 diff 与影响分析

**What to build:** 页面改版后维护者重爬快照，框架与历史基线对比，输出结构化变更清单，并映射到受影响的定位器/页面/flow，明确告知「这次改版要修哪些用例」。这是 AI 生成用例可长期维护的闭环保障。

**Blocked by:** 06（快照基线）、07（flow 与页面/定位器的引用关系）。

> 优先级确认（2026-09-23，SPEC D11）：本票维持主交付地位；近期先落定位器多候选回退（已有）+ 稳定结构断言 + 用例总量控制。

**Status:** ready-for-agent

- [ ] 重爬与历史快照 diff，输出结构化变更清单（新增/删除/变更的元素与路由）
- [ ] 变更映射到受影响的定位器条目、Page Object、flow 清单
- [ ] diff 结果写入报告/文档，可直接作为修复任务的输入
- [ ] 验收：人为改动一个页面元素后重爬，影响清单准确命中所有引用它的 flow
