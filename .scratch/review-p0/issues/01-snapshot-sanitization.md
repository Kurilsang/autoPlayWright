# 01: 快照产物脱敏与入库兜底

**What to build:** 产品安全负责人确信快照产物不会泄露敏感信息：所有爬虫/快照产物在落盘前自动经过脱敏器，产品名、个人账号、内网主机等敏感串替换为约定占位符，映射表可配置扩展；已入库的当前快照产物完成脱敏改写；文本类快照（JSON/HTML）对 git 兜底不可见。脱敏只替换敏感串，快照的结构、信号时间线、状态字段与截图引用完整保留。

**Blocked by:** None (can start immediately)

**Status:** done

- [x] 脱敏纯函数：含产品名/账号/内网主机的文本 → 敏感串全部替换为占位符
- [x] 无敏感内容的文本 → 原样返回
- [x] 映射表新增条目 → 不改代码即生效（配置驱动）
- [x] 脱敏后快照的结构/信号/状态字段与截图引用保留（结构对拍不变）
- [x] 快照落盘链路全部接入脱敏器（状态快照与探查产物两条产线）
- [x] 已入库的当前快照产物完成脱敏改写，现树全文检索不到敏感串
- [x] 忽略清单覆盖文本类快照（JSON/HTML），与既有 PNG 忽略并列
- [x] 既有爬虫用例回归全绿

## Comments
- 2026-09-24 交付：`apw.sanitize` 纯函数（词边界映射 + 通用模式兜底）+ 快照/探查双产线接线 + 40 个存量产物清扫；全仓 git 可见面扫描残留为 0（测试夹具假名为 `taylor.smith`/`example.com`）。
- 已入库 4 个快照 JSON 按票面「脱敏改写」处置：敏感串替换为占位符后保留入库（每文件约 10 行改写）；新增文本类快照（JSON/HTML）由忽略清单兜底不入库。git 历史旧版本是否改写仍留人工决定（见 spec Further Notes）。
- 复核加强：词边界替换防 `yaml` 被键 `aml` 误伤；无点句柄（`@property` 类）不误伤。
