# 06: run 目录唯一化与汇总合并

**What to build:** CI 使用者确信并行 worker 与同秒重跑互不覆盖：run 目录标识保留时间戳可读性并叠加进程/随机熵后缀保证全局唯一；汇总写入按已有明细合并而非覆盖并行产物。

**Blocked by:** 05（flow 三态与跳过留痕——汇总合并要合三态统计，先定统计语义避免返工）

**Status:** done

- [x] 连续两次构建报告器 → 产物目录不重名
- [x] 并行进程各写各的明细，run 目录互不冲突
- [x] 汇总写入保留既有/并行明细（合并语义），不覆盖
- [x] 目录名保留时间戳前缀可读性
- [x] 既有报告用例回归全绿

## Comments
- 2026-09-24 交付：run_id = `YYYYmmdd-HHMMSS-<pid><熵>`（唯一性由 pid+熵构造性保证，同秒/跨进程不撞）；`_merge_flows` 合并语义（同 flow_id 以本次为准，计数从合并集重算）。
- code-review 复核修正：删除 `run_id` 显式参数（两轴均判超范围的 API 扩张），合并测试改以共享 out_dir 模拟并行 writer；「并行进程」验收的证据为构造性保证 + 同进程双构建实测。
