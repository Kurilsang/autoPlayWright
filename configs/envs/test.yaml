# 真实测试环境（Aml Agent）。凭据不入库：secrets.local.yaml（本地）或环境变量（CI）。
base_url: https://aml-agent.amlogic.com/chat
driver:
  mode: web
  headless: true
auth:
  type: form
  login_url: https://aml-agent.amlogic.com/login
  login_path_hint: /login
  username_selector: "#username"
  password_selector: "#password"
  submit_selector: 'button[type="submit"]'
  state_file: configs/auth-state.json   # 登录成功后回写，后续运行复用会话
  secrets_file: configs/secrets.local.yaml
